# ====================================================================
# VPN MANAGEMENT TOOL - SENSITIVE FILES
# ====================================================================

# 敏感配置文件
.vpn_config
.user_vpn_config
*.key
*.crt
*.pem

# 配置文件 - 包含敏感信息 (AWS account IDs, VPC IDs, etc.)
configs/**/*.env
configs/**/*.conf
*.env

# IAM policies with hardcoded account IDs
iam-policies/*.json

# Certificate files and VPN configurations
certs/**/*.key
certs/**/*.crt  
certs/**/*.pem
certs/**/*.ovpn

# 日誌文件
*.log

# Deployment history files
.slack-urls-history
.production-url

# 測試結果文件
*test_results*.txt

# 證書目錄
certificates/
user-certificates/
team-configs/
vpn-config/
offboarding-logs/
revocation-logs/

# AWS 配置文件 (如果意外複製到專案目錄)
.aws/

# 執行時生成的文件
pki/
*.ovpn
.current_env
certs/
.claude/settings.local.json
configs/staging/*.env.backup.*
*chatlog.json
configs/**/vpn_endpoints.json
configs_backup*/
configs/**/vpn_endpoint.conf.backup*
configs/staging/vpn_endpoint.conf.bak
security_group_commands_*.sh
.production-url

# ====================================================================
# CDK (AWS CLOUD DEVELOPMENT KIT) SPECIFIC
# ====================================================================

# CDK build outputs
cdklib/cdk.out/
cdklib/dist/
cdklib/build/
cdklib/lib/**/*.js
cdklib/lib/**/*.js.map
cdklib/lib/**/*.d.ts
cdklib/bin/**/*.js
cdklib/bin/**/*.js.map
cdklib/bin/**/*.d.ts

# CDK dependencies
cdklib/node_modules/
cdklib/package-lock.json

# CDK context and cache
cdklib/cdk.context.json
cdklib/.cdk.staging/

# CDK deployment artifacts
cdklib/asset.*/
cdklib/cdk-outputs.json
cdklib/cdk-exports.json

# CDK bootstrap information
cdklib/bootstrap-template.yaml
cdklib/cdk-toolkit-stack.yaml

# CDK debugging and logs
cdklib/cdk.log
cdklib/debug.log
cdklib/npm-debug.log*
cdklib/yarn-debug.log*
cdklib/yarn-error.log*

# CDK temporary files
cdklib/.tmp/
cdklib/tmp/
cdklib/*.tmp
cdklib/*.temp

# CDK test coverage
cdklib/coverage/
cdklib/.nyc_output/
cdklib/test-results.xml
cdklib/jest-report.html

# CDK environment files
cdklib/.env
cdklib/.env.local
cdklib/.env.*.local

# ====================================================================
# LAMBDA/NODE.JS PROJECT SPECIFIC
# ====================================================================

# Lambda dependencies
lambda/node_modules/
lambda/*/node_modules/
lambda/package-lock.json
lambda/*/package-lock.json

# Lambda build outputs
lambda/dist/
lambda/*/dist/
lambda/build/
lambda/*/build/
lambda/coverage/
lambda/*/coverage/
lambda/.nyc_output/
lambda/*/.nyc_output/

# TypeScript 編譯輸出
lambda/**/*.js
lambda/**/*.js.map
lambda/**/*.d.ts
!lambda/jest.config.js
!lambda/jest.setup.js

# Lambda 部署和打包文件
lambda/*.zip
lambda/*/*.zip
lambda/template-generated.yaml
lambda/samconfig.toml
lambda/.aws-sam/

# Lambda build artifacts
layer-package/
layer-new.zip
slack-handler-new.zip
test-lambda-direct.json
response-lambda*.json
lambda/shared/layer.zip
lambda/shared/nodejs/

# 測試報告和快照
lambda/test-results.xml
lambda/*/test-results.xml
lambda/jest-report.html
lambda/*/jest-report.html
lambda/**/__tests__/**/*.snap

# 環境變數和機密配置
lambda/.env
lambda/*/.env
lambda/.env.local
lambda/*/.env.local
lambda/.env.*.local
lambda/*/.env.*.local

# AWS 相關
lambda/.aws/
lambda/*/.aws/
lambda/aws-exports.js
lambda/*/aws-exports.js

# CDK 輸出
lambda/cdk.out/
lambda/*/cdk.out/

# 日誌和除錯文件
lambda/**/*.log
lambda/**/npm-debug.log*
lambda/**/yarn-debug.log*
lambda/**/yarn-error.log*
lambda/**/lerna-debug.log*

# ====================================================================
# SYSTEM AND EDITOR FILES
# ====================================================================

# macOS 系統文件
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# 臨時文件
*.tmp
*.temp
*.swp
*.swo
*~
*.tar.gz.enc

# 編輯器文件
.vscode/
.idea/
*.sublime-*

# ====================================================================
# ADDITIONAL CDK BEST PRACTICES
# ====================================================================

# CDK version lock files (optional - some teams prefer to commit these)
# Uncomment if you want to ignore version locks
# cdklib/cdk.json.lock
# lambda/package-lock.json

# CDK diff outputs
cdklib/cdk.diff
cdklib/*.diff

# CDK synth outputs (if not using cdk.out)
cdklib/synth/
cdklib/synthesized/

# CDK asset staging
cdklib/asset.*/
cdklib/cdk.staging/

# CDK metadata
cdklib/cdk.metadata.json

# CDK hotswap cache
cdklib/.cdk-hotswap-cache/
configs/**/vpn_endpoint.conf.bak*

