# Production Environment Configuration Example
# Copy this file to configs/production/production.env and modify for your production environment

# Environment identification
ENV_NAME=production
ENV_DISPLAY_NAME="Production Environment"
ENV_COLOR=red
ENV_ICON=🔴

# AWS settings - CRITICAL: This determines which AWS account/profile is used
AWS_REGION=ap-northeast-1
AWS_PROFILE=production-vpn-admin
# Alternative profile name mapping (if your AWS profile has a different name)
# ENV_AWS_PROFILE=production

# VPN configuration
VPN_CIDR=172.20.0.0/22
VPN_NAME=Production-VPN
# ENDPOINT_ID will be set automatically when VPN endpoint is created
# ENDPOINT_ID=cvpn-endpoint-prod456

# Network settings - These should match your production AWS environment
PRIMARY_VPC_ID=vpc-prod456
PRIMARY_VPC_CIDR=10.0.0.0/16
PRIMARY_SUBNET_ID=subnet-prod456

# Directory paths (relative to project root)
CERT_DIR="./certs/production"
CONFIG_DIR="./configs/production"
LOG_DIR="./logs/production"

# Security settings
REQUIRE_MFA_FOR_ADMIN=true
ENABLE_AUDIT_LOGGING=true
REQUIRE_OPERATION_CONFIRMATION=true

# Account validation (for cross-account safety)
# PRODUCTION_ACCOUNT_ID=222222222222  # Optional: AWS Account ID for validation