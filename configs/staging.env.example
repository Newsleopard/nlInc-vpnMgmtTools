# Staging Environment Configuration Example
# Copy this file to configs/staging/staging.env and modify for your staging environment

# Environment identification
ENV_NAME=staging
ENV_DISPLAY_NAME="Staging Environment"
ENV_COLOR=yellow
ENV_ICON=🟡

# AWS settings - CRITICAL: This determines which AWS account/profile is used
AWS_REGION=ap-northeast-1
AWS_PROFILE=staging-vpn-admin
# Alternative profile name mapping (if your AWS profile has a different name)
# ENV_AWS_PROFILE=staging

# VPN configuration
VPN_CIDR=172.16.0.0/22
VPN_NAME=Staging-VPN
# ENDPOINT_ID will be set automatically when VPN endpoint is created
# ENDPOINT_ID=cvpn-endpoint-staging123

# Network settings - These should match your staging AWS environment
PRIMARY_VPC_ID=vpc-staging123
PRIMARY_VPC_CIDR=10.1.0.0/16
PRIMARY_SUBNET_ID=subnet-staging123

# Directory paths (relative to project root)
CERT_DIR="./certs/staging"
CONFIG_DIR="./configs/staging"
LOG_DIR="./logs/staging"

# Security settings
REQUIRE_MFA_FOR_ADMIN=false
ENABLE_AUDIT_LOGGING=true
REQUIRE_OPERATION_CONFIRMATION=true

# Account validation (for cross-account safety)
# STAGING_ACCOUNT_ID=111111111111  # Optional: AWS Account ID for validation